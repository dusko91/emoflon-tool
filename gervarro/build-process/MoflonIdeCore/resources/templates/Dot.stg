group dot;

root_graph(name, entries) ::= <<

digraph <name> { 
	<entries>
}
>>

graph_lines(name, entries) ::= <<

subgraph <name> { 
	<entries>
}
>>

node_line(name, argument) ::= << 
<name><argument>;
>>

args_line(arguments) ::= <<
[<arguments;separator="][">]
>>